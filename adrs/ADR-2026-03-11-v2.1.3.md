**Architectural Decision – Default Coder Model Tag Hardening: nemotron-3-nano:30b-a3b-q4_K_M (v2.1.4)**

**Status:** Approved – paranoid provenance + hardware realism preserved.  
**Rationale (maniacal security & auditing focus):**  
The model name is user-controlled via `ollama` skill pulls, but SKILL.md files set the **recommended / example** tag. Choosing the correct one avoids confusion, failed pulls, and accidental use of weaker/older variants. All changes remain config-only — zero impact on `network_policy`, mounts, `cap_drop`, audit chaining, or TCB size. Every pull is still audited with full `outbound` allow-list visibility in `./shared/audit/seedclaw.log`.

**Final recommendation for RTX 3060-class (12 GB VRAM) hardware:**

**Use `nemotron-3-nano:30b-a3b-q4_K_M`**

**Why not `nemotron-3-nano:30b-q4_K_M`?**

From Ollama library tags and model metadata (March 2026):

- The **canonical, high-quality MoE variant** (30B total params / ~3.6B active) is tagged with **`-a3b`**  
  → `30b-a3b-q4_K_M` = the real Nemotron-3-Nano-30B-A3B quantized to Q4_K_M (~24 GB file size, fits comfortably with layer offloading on 12 GB VRAM)  
- `nemotron-3-nano:30b` (without `-a3b`) exists as a tag, but it is either an alias or a legacy/non-preferred variant. Community & NVIDIA references overwhelmingly point to **`-a3b`** for the released, benchmarked 30B-A3B model (hybrid Mamba-Transformer MoE, 1M context, strong agentic/reasoning).  
- No evidence of a meaningful separate `:30b-q4_K_M` (non-a3b) that outperforms or differs significantly from the `-a3b` line. Using the non-`a3b` tag risks pulling something older or misaligned.

**Hardware & performance reality check (RTX 3060 12 GB):**

- **File size / VRAM usage:** ~24 GB quantized model → Ollama + llama.cpp back-end will offload most layers to system RAM (16–32 GB recommended host RAM). Expect 8–18 t/s at 4k–8k context (very usable for SeedClaw’s ReAct + code-gen loops).
- **1M context window** remains available (huge win when feeding long ARCHITECTURE.md + PRD.md + SKILL.md chains to the coder skill).
- **Q4_K_M** is the sweet-spot balance: good quality vs memory footprint. Avoid q8_0 (~34 GB) unless you have 24 GB VRAM + headroom.

**Security & auditing invariants (unchanged & trivially verifiable):**

- `coder` skill → `"outbound": "none"` (no model name affects this)
- `ollama` skill → narrow `"allow_list"` only for `registry.ollama.ai` / `ollama.com:443`
- Memory exception for ollama container (`mem_limit: 16g` + `shm_size: 1g`) stays hardcoded & audited in registry + log
- Trivial grep still works:  
  `grep -E 'nemotron|30b-a3b|mem_limit|pull' shared/audit/seedclaw.log` shows model choice + exception

**Bootstrap / upgrade workflow (zero surprises):**

1. Apply the two textual changes above.
2. If already running → send message to `ollama` skill:  
   `{"action": "pull", "model": "nemotron-3-nano:30b-a3b-q4_K_M"}`
3. (Optional) Update any bootstrap-prompt.md examples to match.
4. Re-run any skill generation that relies on coder → new generations now default to stronger, more trustworthy base model.
5. Audit trail captures everything: model pull timestamp, hash chain, no network policy violation.

This keeps SeedClaw paranoid, auditable, and realistically runnable on consumer RTX 3060 hardware while maximizing trust in the underlying model provenance.
